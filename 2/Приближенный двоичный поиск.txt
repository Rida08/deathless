#include <iostream>
#include <string>

int main()
{
	long long n, k;
	std::cin >> n >> k;
	int* a = new int[n];
	int* b = new int[k];
	for (int i = 0; i < n; i++)
		std::cin >> a[i];
	for (int i = 0; i < k; i++)
		std::cin >> b[i];
	for (int i = 0; i < k; i++)
	{
		long long l = 0, r = n, m = n / 2;
		while (r - l > 1){
			a[m] < b[i] ? l = m : r = m;
			m = (l + r) / 2;
		}
		std::cout << a[l] <<"\n";
	}
}

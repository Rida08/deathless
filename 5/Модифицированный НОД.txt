
#include <iostream>
#include <string>
#include <algorithm>
#include <vector>

using namespace std;
using ll = long long;

ll gcd(ll a, ll b){
	if (b == 0)
		return a;
	return gcd(b, a % b);
}

int main(){
	int a, b, n, l, h;
	cin >> a >> b >> n;
	string an = "";
	vector<int>pr;

	for (int i = 1; i * i <= min(a, b); ++i) {
		if (a % i == 0 && b % i == 0) {
			pr.push_back(i);
			if (i * i != a && b%(a/i)==0) {
				pr.push_back(a / i);
			}
		}
	}

	sort(pr.begin(), pr.end());

	for (int j = 0; j < n; ++j)	{
		cin >> l >> h;
		int l1 = -1, r = pr.size()-1;
		while (l1 + 1 < r) {
			int m = (l1 + r) / 2;
			if (pr[m] > h) {
				r = m;
			}else {z
				l1 = m;
			}
		}
		if (pr[r] <= h && pr[r]>=l) {
			an += to_string(pr[r]) + "\n";
		}else {
			if (pr[l1] <= h && pr[l1] >= l) {
				an += to_string(pr[l1]) + "\n";
			}else {
				an += to_string(-1) + "\n";
			}
		}
	}
	cout << an;
}

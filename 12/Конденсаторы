#include <iostream>
#include <vector>
#include <string>
#include <algorithm>
#include <cmath>

using namespace std;
using ll = long long;

int n;
vector<int> m;
bool us[10];
double c;
string an = "NO";

void dt(int k, double j) {
    cout << j << " ";
    if (abs(j - c) <= 0.01) {
        an = "YES";
        return;
    }
    if (k > n) return;
    for (int i = 0; i < n; ++i) {
        if (!us[i]) {
            us[i]=1;
            double n1 = (double)j + m[i];
            double n2 = (double)j * m[i] / (n1);
            dt(k++, n1);
            dt(k++, n2);
            us[i]=0;
        }
    }
}

int main() {
    cin >> n >> c;
    int x;
    for (int i = 0; i < n; ++i) {
        cin >> x;
        m.push_back(x);
    }
    for (int i = 0; i < n; ++i) {
        us[i] = 1;
        dt(1, m[i]);
        us[i] = 0;

    }
    cout << an;
}

#include <iostream>
#include <cmath>
#include <iomanip>
using namespace std;
int main()
{
	double c;
	cin >> c;
	double l = 0, r = 1e9, m = r / 2;
	for (int i = 0; i < 60; ++i) {
		m*m+sqrt(m) > c ? r = m : l = m;
		m = (l + r) / 2;
	}
	cout <<setprecision(14)<<fixed<< r;
}
